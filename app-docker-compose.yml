version: '3'

services:
  app:
    container_name: app_red21
    build: .
    ports:
      - "8121:8080"
    restart: unless-stopped
    logging:
      driver: "json-file"
    networks:
      - demo-network

networks:
  demo-network:
    external: true
